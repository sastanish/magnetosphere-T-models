 - Does the current smoothing of the Neutron Monitors actually remove some of the features that we care about?
 - Why does the TS05 model use a smoothed tilt angle whereas the TA16 model uses the full tilt angle?

